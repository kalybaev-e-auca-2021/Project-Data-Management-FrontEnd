import React from 'react';
import EmployeesTable from './EmployeesTable'; // Import the new EmployeesTable component

const Employees = () => {
    return (
        <div>
            <EmployeesTable />
        </div>
    );
};

export default Employees;
